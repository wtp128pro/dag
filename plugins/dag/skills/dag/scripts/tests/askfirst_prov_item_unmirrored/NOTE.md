askfirst_prov_item_unmirrored (1.9.0): a definition_of_done_provenance entry claims item 'DoD-Z' whose register_ids [1] DO resolve (so the AF-17 dangling-register_ids arm is silent), but 'DoD-Z' is not a verbatim member of its mirror anchor list definition_of_done ["DoD-A"]. Exit 1 via I38 AF-17 `provenance dangling` item-not-in-mirror sub-condition — the residual clause the panel flagged as un-pinned (askfirst_prov_dangling pins only the register_ids arm). T2 adoption arm, fires below ship (archive/adoption independence).
