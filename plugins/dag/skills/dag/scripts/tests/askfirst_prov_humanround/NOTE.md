askfirst_prov_humanround (1.9.0): a definition_of_done_provenance entry claims source 'human-round' but carries NO round_ref (the round_ref key is absent, i.e. blank) — a human-round attribution with no dialogue round to point at. Exit 1 via I38 AF-45 `provenance attribution` source=human-round arm. Distinct from askfirst_prov_round_dangling (which pins the round_ref-present-but-unresolving arm) and askfirst_prov_attribution (which pins the register-row-no-ids arm); this pins the `source == "human-round" and blank(round_ref)` branch. T2 adoption arm, fires below ship.
