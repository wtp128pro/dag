askfirst_halt_after_work: the park-and-ship forge (LP-13). A well-formed marker lists an open material CC row, but SYNTHESIS.md (completed-work evidence) is on disk. Exit 1 via I38 AF-42 `halt after work`; AF-18's marker exemption is halt-freshness-void so `open consequential` also fires, and I8 fires on the material open row — a DIFFERENT line set than the sanctioned halt (machine-separable).
