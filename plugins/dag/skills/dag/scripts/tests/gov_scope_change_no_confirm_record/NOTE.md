gov_scope_change_no_confirm_record: a scope_change add_units amendment (A01) DOES carry a transcript_ref (so the GV-21 no-transcript arm is silent), and DoD-NEW IS human-confirmed (a `confirmed` item_confirmations record with a transcript_ref, so I39 stays clean) — but there is NO `added` item_confirmations record whose amendment_ref references A01. Exit 1 via I40-7 (GV-21) `scope_change no confirmation` sub-clause. Distinct from gov_scope_change_no_confirm, which pins the `no transcript` sub-clause; this pins the confirmation-record sub-clause. Adoption-armed on item_confirmations presence (fires at any version; unstamped like its sibling).
